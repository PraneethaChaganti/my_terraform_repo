pipeline{
  agent any

    stages{
      stage('terraform steps'){
        steps {
            sh '''
              cd Terraform
              terraform init
              terraform plan
              terraform apply -auto-approve
              terraform output
            '''
        }
      }
    }
}
