pipeline{
  agent any

    stages{
      stage('terraform steps'){
        steps {
            dir('cd /var/jenkins_home/workspace/Jenkins_Github_Pipeline/Terraform'){
                sh 'terraform init'
                sh 'terraform plan'
                sh 'terraform apply -auto-approve'
                sh 'terraform output'
            }
        }
      }
    }
}