pipeline{
  agent any

    stages{
      stage('terraform steps'){
        steps {
            sh '''
              cd /var/jenkins_home/workspace/Jenkins_Github_Pipeline/Terraform
              terraform init
              terraform plan
              terraform apply -auto-approve
              terraform output
            '''
        }
      }
    }
}
